<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Réseau -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- Caméra & audio -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Stockage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Localisation -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Overlay (indicateur enregistrement) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- SDK BYD propriétaire -->
    <uses-permission android:name="com.byd.permission.BYDAUTO_PANORAMA" />
    <uses-permission android:name="com.byd.permission.BYDAUTO_BODYWORK" />
    <uses-permission android:name="com.byd.permission.BYDAUTO_BMS" />
    <uses-permission android:name="com.byd.permission.BYDAUTO_ENGINE" />

    <!-- Privilèges système (accordés via ADB) -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />

    <application
        android:name=".BydCamApplication"
        android:label="BydCam"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.BydCam"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true">

        <!-- Activité principale -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activité diagnostic Binder BYD (debug uniquement) -->
        <activity
            android:name=".diag.DiagnosticActivity"
            android:exported="true"
            android:label="BydCam Diag"
            android:screenOrientation="landscape" />

        <!-- Service foreground principal (caméra + enregistrement) -->
        <service
            android:name=".services.CameraService"
            android:exported="false"
            android:foregroundServiceType="camera|mediaProjection" />

        <!-- Service sentinelle (surveillance à l'arrêt) -->
        <service
            android:name=".services.SentryService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- Démarrage automatique au boot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <!-- ACC ON/OFF BYD -->
                <action android:name="com.byd.intent.action.ACC_ON" />
                <action android:name="com.byd.intent.action.ACC_OFF" />
            </intent-filter>
        </receiver>

        <!-- FileProvider pour partage de clips -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
